import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'PowerFit Gym - Transform Your Body & Mind',
  description: 'Modern gym offering strength training, boxing, CrossFit, bootcamp, and wellness programs. Join now for premium facilities and expert personal trainers.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
